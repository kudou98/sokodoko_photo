<% if @user_favorites.present? %>
  <% @user_favorites.each do |favorite| %>
    <%= image_tag post.get_image, class:"card-img-top"; %>
    <%= image_tag post.user.get_profile_image(100,100), class:"mt-3 rounded-circle" %>
    <%= post.location %>
    <%= post.body %>
    <%= post.user.name %>
    <%= link_to "#{post.post_comments.count} コメント", post_path(post.id), class:"text-dark" %>
    <%= link_to post.tag, search_post_path(keyword: post.tag) %>
    <% end %>
<% end %>